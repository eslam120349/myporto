{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/sitemap", "destination": "/sitemap.xml" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
